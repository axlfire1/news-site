<%= form_with(model: news_wall) do |form| %>
  <% if news_wall.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(news_wall.errors.count, "error") %> prohibited this news_wall from being saved:</h2>

      <ul>
        <% news_wall.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
